import React from 'react';

const Services = () => {
  const containerStyle = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
    backgroundColor: '#f0f0f0',
    fontSize: '24px',
    color: '#333',
    textAlign: 'center',
  };

  return (
    <div style={containerStyle}>
      Be back under construction
    </div>
  );
};

export default Services;
